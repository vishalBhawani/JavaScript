document.addEventListener('DOMContentLoaded', () => {

    // const str = "Vishal";
    // //const strr = str.slice(4, 7)
    // //console.log(strr);


    // // Convert string to array
    // const str1 = Array.from(str);

    // console.log(str1);

    // // access str1 array

    // for (i in str1) {
    //     console.log(str1[i])
    // }

    let strw = "aello";
    console.log(strw.charCodeAt(0)); // 'H'

});